<!DOCTYPE html>
<html>
<head>
  <title>Ninkatec Onboarding | Form 5</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <style>
    .video-container {
      max-width: 300px;
      margin-bottom: 10px;
    }
    .video-container video {
      max-width: 300px;
      max-height: 180px;
      width: 100%;
      height: auto;
    }
  </style>
</head>

<body>
  <div class="container">
    <!-- Steps Bar -->
    <div class="steps">
      <div class="page">
        <!-- Steps bar content will go here -->
      </div>
    </div>

    <!-- Title -->
    <h1>Environmental Analysis</h1>
        
    <!-- Video Upload Section -->
    <%= form_with(model: @form, url: edit_5_form_path(@form), method: :patch, local: true, html: { multipart: true }) do |form| %>
      <div class="row">
        <!-- Step 1: Environment Video -->
        <div class="box-video g-5">
          <h2 style="text-align: center">Environmental Video</h2>
          <hr/>
          <p style="text-align: center">
            Take a video from your house doorstep including the Living Room, Bedroom, and Toilet. The video should show the patient moving around the house as they would daily.
          </p>
          <%= form.file_field :environment_video, accept: 'video/*', class: 'form-control mb-3'%>
          <%= form.submit 'Upload', name: 'commit', value: 'Upload Environment Video', class: 'btn btn-primary' %>
          <div class="video-container text-center">
            <% if @form.environment_video.attached? %>
              <p>Current Environment Video:</p>
              <%= video_tag url_for(@form.environment_video), controls: true, class: "img-thumbnail" %>
            <% end %>
          </div>
        </div>
      </div>

      <!-- Navigation Buttons -->
      <div class="d-flex justify-content-center mt-5">
        <%= link_to "Back", edit_4_form_path(@form), class: "btn btn-secondary me-2" %>
        <%= form.submit "Save", name: "commit", value: "Save", class: "btn btn-success me-2" %>
        <%= form.submit "Next",  name: "commit", value: "Submit", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>