<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Meeting</title>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .container {
            max-width: 600px;
            margin-top: 2em;
        }
        .header-container {
            display: flex;
            align-items: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <%= form_with(model: @meeting, local: true, html: { class: "mt-4" }) do |form| %>
            <% if @meeting.errors.any? %>
                <div class="alert alert-danger">
                    <h4><%= pluralize(@meeting.errors.count, "error") %> prohibited this meeting from being saved:</h4>
                    <ul>
                        <% @meeting.errors.each do |error| %>
                            <li><%= error.full_message %></li>
                        <% end %>
                    </ul>
                </div>
            <% end %>

            <div class="mb-3">
                <%= form.label :title do %>
                    Meeting Title/Patient Name <span style="color: red;">*</span> 
                <% end %>
                <%= form.text_field :title, required: true, class: "form-control" %>
            </div>

            <div class="mb-3">
                <%= form.label :description %>
                <%= form.text_area :description, class: "form-control" %>
            </div>

            <div class="mb-3">
                <%= form.label :location do %>
                    Meeting Location <span style="color: red;">*</span> 
                <% end %>
                <%= form.text_area :location, required: true, class: "form-control" %>
            </div>

            <div class="mb-3">
                <%= form.label :start_time do %>
                    Meeting Start Time <span style="color: red;">*</span> 
                <% end %>
                <%= form.datetime_local_field :start_time, required: true, class: "form-control" %>
            </div>

            <div class="d-grid gap-2">
                <%= form.submit "Save Changes", class: "btn btn-success" %>
            </div>
        <% end %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
