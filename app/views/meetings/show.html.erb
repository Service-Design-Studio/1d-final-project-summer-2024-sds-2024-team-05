<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meeting Details</title>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        .container {
            max-width: 800px;
            margin-top: 2em;
        }
        .header-container {
            display: flex;
            align-items: center;
        }
        .icon-link {
            display: flex;
            align-items: center;
            text-decoration: none;
            color: inherit;
            margin-right: 10px;
        }
        .icon-link:hover {
            color: #0056b3;
        }
        
        p {
          font-size: 1.5em;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header-container mb-1 justify-content-between">
            <h1>Meeting Details</h1>
            <div class="d-flex justify-content-end">
                <%= link_to edit_meeting_path(@meeting), class: "icon-link btn btn-outline-primary" do %>
                    <i class="fas fa-edit"></i> Edit
                <% end %>
                <%= button_to @meeting, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger" do %>
                    <i class="fas fa-trash-alt"></i> Delete
                <% end %>
            </div>
        </div>

        <hr/>

        <div class = "d-flex justify-content-between align-items-center">
          <h2 style = "margin-bottom: 0.5em;">Patient Name: <%= @meeting.title%></h2>
              <% if @meeting.form %>
                  <%= link_to client_profile_form_path(@meeting.form), class: "icon-link btn btn-secondary text-light", style:"margin-right: 0" do %>
                      <i class="fas fa-user"></i> Client Information
                  <% end %>
              <% end %>
        </div>

        <p class="card-text">
            <strong>Description:</strong> <%= @meeting.description %>
        </p>
        <p class="card-text">
            <strong>Location:</strong> <%= @meeting.location %>
        </p>
        <p class="card-text">
            <strong>Start time:</strong> <%= @meeting.start_time %>
        </p>
            </div>
        </div>
      </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
