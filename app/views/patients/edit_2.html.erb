<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Edit Form - Step 2</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>
<body>
  <div class="container">
    <div class="steps">
      <div class="page">
        <p class="numbers-not-active">1</p>
        <p class="dash">---</p>
        <p class="numbers-active">2</p>
        <p class="dash">---</p>
        <p class="numbers-not-active">3</p>
        <p class="dash">---</p>
        <p class="numbers-not-active">4</p>
        <p class="dash">---</p>
        <p class="numbers-not-active">5</p>
        <p class="dash">---</p>
        <p class="numbers-not-active">6</p>
      </div>
    </div>
    <h1><%= @form_origin_text %> - Step 2</h1>
    
    <%= form_with(model: @form, url: edit_2_form_path(@form), method: :patch, local: true) do |form| %>
      <div class="form-group">
        <%= form.label :height %>
        <%= form.number_field :height, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :weight %>
        <%= form.number_field :weight, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :conditions %>
        <%= form.text_field :conditions, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= form.label :medication %>
        <%= form.text_field :medication, class: "form-control" %>
      </div>

      <div class="row d-flex justify-content-left mt-4">
        <div class="col-sm-12">
          <fieldset>
            <p>Has the Patient been Hospitalised Recently?</p>
            <div class="form-check form-check-inline">
              <%= form.radio_button :hospital, '1', class: 'form-check-input', checked: @form.hospital == true %>
              <%= form.label :hospital_yes, "Yes", class: 'form-check-label' %>
            </div>
            <div class="form-check form-check-inline">
              <%= form.radio_button :hospital, '0', class: 'form-check-input', checked: @form.hospital == false %>
              <%= form.label :hospital_no, "No", class: 'form-check-label' %>
            </div>
          </fieldset>
          <p><i>If yes, please upload the relevant discharge summary (optional)</i></p>
        </div>
      </div>

      <%= link_to "Back", edit_1_form_path(@form), class: "btn btn-secondary" %>
      <%= form.submit "Next", class: "btn btn-primary" %>
    <% end %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>