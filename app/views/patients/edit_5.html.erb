<!DOCTYPE html>
<html>
<head>
  <title>Ninkatec Onboarding | Form 5</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>

</head>

<body>
  <div class="container">
    <!-- Steps Bar -->
    <div class="steps">
      <div class="page">

          <%= button_to '1', @form.persisted? ? edit_1_form_path(@form) : edit_1_forms_path, method: :get, class: @form.persisted? ? @valid_button_1_class : "btn btn-primary circular-button btn-outline-blue" %>

          <%= button_to '2', @form.persisted? ? edit_2_form_path(@form) : edit_2_forms_path, method: :get, class: @form.persisted? ? @valid_button_2_class : "btn btn-primary circular-button btn-outline-blue" %>   
         
          <%= button_to '3', @form.persisted? ? edit_3_form_path(@form) : edit_3_forms_path, method: :get, class: @form.persisted? ? @valid_button_3_class : "btn btn-primary circular-button btn-outline-blue" %>
          
          <%= button_to '4', @form.persisted? ? edit_4_form_path(@form) : edit_4_forms_path, method: :get, class: @form.persisted? ? @valid_button_4_class : "btn btn-primary circular-button btn-outline-blue" %>         

          <button type="button" class="btn btn-primary circular-button btn-blue">5</button>
          
          <%= button_to '6', @form.persisted? ? @form : show_error_forms_path, method: :get, class: @form.persisted? ? @valid_button_summ_class : "btn btn-primary circular-button btn-outline-blue" %>
        <!-- Steps bar content will go here -->
      </div>
    </div>

    <!-- Title -->
    <h1>Environmental Analysis</h1>
        
    <!-- Video Upload Section -->
    <%= form_with(model: @form, url: @form.persisted? ? edit_5_form_path(@form) : edit_5_forms_path, method: :patch, local: true, html: { multipart: true }) do |form| %>
      <div class="row">

        <!-- Step 1: Environment Video -->
        <div class="box-video g-5">

          <%# <h2 style="text-align: center">Environmental Video</h2> %>
          
 
          <p style="text-align: center">
            Take a video from your house doorstep including the Living Room, Bedroom, and Toilet. The video should show the patient moving around the house as they would daily.
          </p>
          <div class="video-preview" style="margin-top: 20px;">
              <iframe class="rounded-video" width="70%" height="300" 
                      src="https://www.youtube.com/embed/BbqtjDekrWc" 
                      frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
                      allowfullscreen>
              </iframe>
          </div>
          <%= form.file_field :environment_video, accept: 'video/*', class: 'form-control mb-3', id: 'environment_video'%>
          <%= form.submit 'Upload', name: 'commit', value: 'Upload Environment Video', class: 'btn btn-primary' %>
          <div class=" text-center">
            <% if @form.environment_video.attached? %>
              <p>Current Environment Video:</p>
              <%= video_tag url_for(@form.environment_video), controls: true, class: "img-thumbnail video-preview rounded-video",width:" 100%", height:"10"  %>
            <% end %>
          </div>
        </div>
      </div>

      <div class="modal fade" id="invalidFormModal" tabindex="-1" aria-labelledby="invalidFormModalLabel" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="invalidFormModalLabel">Form Incomplete</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
              Please fill up all required forms.
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
          </div>
        </div>
      </div>

      <!-- Navigation Buttons -->
      <div class="d-flex justify-content-center mt-5 mb-5">
        <%= link_to "Back", @form.persisted? ? edit_4_form_path(@form) : edit_4_forms_path, class: "btn btn-lg btn-outline-black rounded-pill", style: 'float: left; margin-right: 5px;' %>
        <%= form.submit "Save", name: "commit", value: "Save", class: "btn btn-lg rounded-pill btn-green me-2", style: 'float: left; margin-right: 5px;'  %>
        <%= form.submit "Next", name: 'commit', value: 'Next', class:"btn btn-lg btn-blue rounded-pill", style: 'float: left; margin-right: 5px;' %>

      </div>
      
    <% end %>
    <br>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>