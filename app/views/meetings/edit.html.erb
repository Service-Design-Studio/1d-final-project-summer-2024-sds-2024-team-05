<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Meeting</title>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        .container {
            max-width: 800px;
            margin-top: 2em;
        }
        .header-container {
            display: flex;
            align-items: center;
        }
        .icon-link {
            display: flex;
            align-items: center;
            text-decoration: none;
            color: inherit;
            margin-right: 10px;
        }
        .icon-link:hover {
            color: #0056b3;
        }
        p {
            font-size: 1.5em;
        }
    </style>
</head>
  <body>
    <div class="container">
      <div class="header-container mb-1 justify-content-between">
        <%= link_to meeting_path(@meeting, origin: 'edit'), class: 'btn' do %>
          <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" fill="currentColor" class="bi bi-chevron-left" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M11.354 1.646a.5.5 0 0 1 0 .708L5.707 8l5.647 5.646a.5.5 0 0 1-.708.708l-6-6a.5.5 0 0 1 0-.708l6-6a.5.5 0 0 1 .708 0"/>
          </svg>
        <%end%>
        <h1>Edit Meeting</h1>
        <div class="d-flex justify-content-end">
            <%= link_to @meeting, class: "icon-link btn btn-secondary" do %>
                <i class="fas fa-user"></i> Client Information
            <% end %>
        </div>
      </div>

      <hr/>

      <%= render "form", meeting: @meeting %>
      
      <hr/>
      <div>
        <%= link_to "Show this meeting", @meeting, class: "icon-link btn btn-secondary" %>
        <% if current_user.admin? %>
          <%= link_to "Back to Dashboard", admin_root_path, class: "icon-link btn btn-secondary" %>
        <% else %>
          <%= link_to "Back to Dashboard", user_root_path, class: "icon-link btn btn-secondary" %>
        <% end %>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>
