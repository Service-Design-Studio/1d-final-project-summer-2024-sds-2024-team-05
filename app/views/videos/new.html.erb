<!DOCTYPE html>
<html>
<head>
  <title>App1</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
</head>

<body>
  <!-- Nav Bar -->
  <nav class="navbar navbar-expand-lg" style = "background: #141E30;">
    <div class="container-fluid">
      <a class="navbar-brand" style="color: #e3f2fd; font-size: 50px" href="#">Ninkatec</a>
      <p style="margin-top: 0.5em; color: #e3f2fd; text-align: right;"><b>Contact</b><br/>62479247<br/>care@ninkatec.com</p>
    </div>
  </nav>

  <div class="container">
    <!-- Steps Bar -->
    <div class="steps">
      <div class="page">
        <p class="numbers-not-active">1</p>
        <p class="dash">---</p>
        <p class="numbers-not-active">2</p>
        <p class="dash">---</p>
        <p class="numbers-active">3</p>
        <p class="dash">---</p>
        <p class="numbers-not-active">4</p>
      </div>
    </div>

    <!-- Title -->
    <h2>Environmental Analysis</h2>

    <!-- Photo Upload Section -->
    <div class="box1 p-0 pl-3 pr-3">
    <h3 style = "text-align: left">For us to improve the client’s living conditions, we need to assess the living conditions of the client</h3>
    <hr/>
    <h4 style = "padding-bottom: 1em">Take 4 pictures of the areas where the patient spends the most time in their home</h4>
    <div class="row">
      <% 4.times do |i| %>
        <div class="col-md-6 g-5">
          <%= form_with model: Video.new, url: videos_create_path, local: true, html: { multipart: true, id: "photoUploadForm#{i+1}" } do |form| %>
              <div class="mb-3">
                  <%= form.label "photo#{i+1}", "Photo #{i+1}" %>
                  <%= form.file_field "photo#{i+1}", accept: 'image/*', class: 'form-control' %>
              </div>
              <div class="mb-3">
                  <%= form.label "tag#{i+1}", "Tag for Photo #{i+1}" %>
                  <%= form.text_field "tag#{i+1}", class: 'form-control' %>
              </div>
              <%= form.submit 'Upload', class: 'btn', style: 'background: #141E30; color: white' %>
          <% end %>
        </div>
      <% end %>
    </div>
    </div>
  </div>

  <!-- Navigation Buttons -->
  <div class="d-flex justify-content-center mb-5">
    <button class="btn btn-outline-dark mt-5 mx-2">Save</button>
    <button class="btn mt-5 mx-2" style = "text-decoration:none; color:white; background: #141E30;"><%= link_to "Continue", new_photos_path, style:"color: inherit; text-decoration:none;" %></button>
  </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
